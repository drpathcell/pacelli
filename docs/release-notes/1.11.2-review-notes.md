No account is required - tap "Continue as guest" on the welcome screen and the
app is fully usable.

This is a maintenance release with no new screens and no visual changes
beyond wording on one settings screen.

To test:

1. Settings -> Delete data -> "Delete my account". Account deletion works
   exactly as before (Guideline 5.1.1(v)). What changed is only that a
   household whose last remaining person leaves is cleaned up completely,
   including any AI assistant that had been connected to it.

2. Settings -> "Who can erase shared data" (household founder only). The
   explanatory text was rewritten to describe the setting precisely.

Notes for review:

- No new permissions, no new data collected, no purchases.
- The screenshots are unchanged from 1.11.0.
