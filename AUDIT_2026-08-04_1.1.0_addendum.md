# Pacelli security audit — 1.1.0 addendum (2026-08-04)

Scope: changes since AUDIT_2026-08-01_native.md — ExportService (new),
HouseholdService.renameHousehold (new), theme rename, SettingsView export UI.
Base audit findings unchanged; crypto/rules/auth untouched this release.

## Gate
- [PASS] PacelliKit cross-language vectors: swift test 28/28 (2026-08-04 16:44).

## Export (ExportService.swift + SettingsView.swift)
- [PASS] Data sourced exclusively from existing repositories — no new
  decrypt paths, no direct Firestore reads.
- [PASS] No key material in the artifact: generated export inspected —
  no key-like JSON keys, no 64-hex blobs, no encrypted_key, no tokens.
  (One "password" hit = user content: manual entry titled "Wifi password".)
- [PASS] All content fields decrypted (0 "[encrypted]" markers).
- [PASS] Plaintext export is user-initiated behind an explicit warning
  alert ("not encrypted — keep it somewhere safe").
- [PASS] Temp files auto-deleted: sheet onDismiss removes every
  "Pacelli export*" from tmp — verified empty on-sim after E2E run.
- [PASS] No print() of exported content or errors carrying data.
- [PASS] File written atomically to app-sandbox tmp only (not backed up,
  not world-readable); leaves the sandbox only via the user's share action.
- [OPEN — 1.2] Optional passphrase-encrypted export variant (skill §5
  criterion) — deliberate product decision to ship plaintext-with-warning
  first; revisit with import.
- [NOTE] Import (restore) not implemented — schema_version: 1 envelope is
  the forward contract; importer must validate structure + remap IDs.

## Household rename (HouseholdService.renameHousehold)
- [PASS] Name encrypted with the household key before updateData — same
  path as createHousehold; empty/whitespace guarded (AES empty-string crash
  class avoided).
- [PASS] Server-side authz: firestore.rules households update requires
  isMember — any-member rename is rule-backed, not client-trust.
- [PASS] AppState.householdRenamed only mutates local session state.

## Theme rename (AppTheme.swift)
- [PASS] Display-only; rawValues keep legacy persistence keys ("claude",
  "gemini") — local @AppStorage, never in Firestore. Trademark exposure
  removed from UI (5.2.1).

## Privacy & Encryption screen
- [PASS] Lists unchanged and still literally true — export does not alter
  what is encrypted at rest.

Verdict: PASS. Fix-order queue empty; one OPEN item carried to 1.2.
