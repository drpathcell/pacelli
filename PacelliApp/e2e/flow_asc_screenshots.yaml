appId: com.pacelli.pacelli
---
# ASC screenshot set (6.9" / iPhone 17 Pro Max, 1320×2868).
# Seeds a fresh guest household with presentable demo data, then captures.
# Set the status bar first:
#   xcrun simctl status_bar <SIM> override --time "9:41" --batteryState charged --batteryLevel 100 --wifiBars 3
- launchApp
- runFlow:
    when:
      visible: "Continue as guest"
    commands:
      - tapOn: "Continue as guest"
- extendedWaitUntil:
    visible: "New task"
    timeout: 45000
# ── Seed tasks ──
- tapOn: "New task"
- inputText: "Water the plants"
- pressKey: Enter
- extendedWaitUntil:
    visible: "Water the plants"
    timeout: 15000
- tapOn: "New task"
- inputText: "Book dentist appointment"
- pressKey: Enter
- extendedWaitUntil:
    visible: "Book dentist appointment"
    timeout: 15000
- tapOn: "New task"
- inputText: "Weekly food shop"
- pressKey: Enter
- extendedWaitUntil:
    visible: "Weekly food shop"
    timeout: 15000
- hideKeyboard
- tapOn: "Water the plants"
# Task detail: category + subtasks
- scrollUntilVisible:
    element: "Add a subtask"
    direction: DOWN
- tapOn: "Add a subtask"
- inputText: "Balcony pots"
- pressKey: Enter
- extendedWaitUntil:
    visible: "Balcony pots"
    timeout: 15000
- hideKeyboard
- tapOn: "Add a subtask"
- inputText: "Kitchen herbs"
- pressKey: Enter
- extendedWaitUntil:
    visible: "Kitchen herbs"
    timeout: 15000
- hideKeyboard
- tapOn: "Balcony pots"
# ── Photos (1.8.0) — NOT CAPTURED YET ──
# An attach was driven here and it worked, but it left the app in a state the
# Checklists section immediately after could not recover from ("Groceries"
# never appeared). Backed out rather than shipped half-working: the set below
# is the known-good one, and the gallery shot is still owed.
#
# When it goes back in, put it AFTER the checklist and plan sections rather
# than in the middle of the task flow, and take the gallery shot from the
# Search tab where flow_photo_02 already does it reliably.
- takeScreenshot: /tmp/asc_03_task_detail
- tapOn:
    id: "BackButton"
- extendedWaitUntil:
    visible: "New task"
    timeout: 15000
- takeScreenshot: /tmp/asc_01_tasks
# ── Checklists ──
- tapOn: "Checklists"
- tapOn: "New checklist"
- inputText: "Groceries"
- pressKey: Enter
- extendedWaitUntil:
    visible: "Groceries"
    timeout: 15000
- hideKeyboard
- tapOn: "Groceries"
- extendedWaitUntil:
    visible: "Add an item"
    timeout: 15000
- tapOn: "Add an item"
- inputText: "Milk"
- pressKey: Enter
- extendedWaitUntil:
    visible: "Milk"
    timeout: 15000
- hideKeyboard
- tapOn: "Add an item"
- inputText: "Eggs"
- pressKey: Enter
- extendedWaitUntil:
    visible: "Eggs"
    timeout: 15000
- hideKeyboard
- tapOn: "Add an item"
- inputText: "Sourdough bread"
- pressKey: Enter
- extendedWaitUntil:
    visible: "Sourdough bread"
    timeout: 15000
- hideKeyboard
# By accessibility label, which hits the tick circle. Tapping the item TEXT
# now lands in the editable title field and throws the keyboard up over half
# the screenshot — harmless in a test, fatal in a store listing.
- tapOn: "Check Milk"
- hideKeyboard
- takeScreenshot: /tmp/asc_04_checklist
# ── Templates (1.6.0) ──
# Saved from this very list, then shown on the Checklists screen, because the
# Templates section is invisible until one exists — a screenshot of the empty
# state would advertise nothing.
- scrollUntilVisible:
    element: "Save as template"
    direction: DOWN
- tapOn: "Save as template"
- extendedWaitUntil:
    visible: "Template name"
    timeout: 15000
# The field is pre-filled with the checklist's own title. Left alone, the
# shot shows "Groceries" twice — once as a list, once as a template — which
# reads as a duplication bug rather than a feature.
# No tapOn first: SwiftUI's .alert bridges to UIAlertController, which
# focuses its text field and raises the keyboard on presentation. It also
# drops any accessibilityIdentifier on the way across, so there is nothing
# here to tap by id even if a tap were needed.
- eraseText
- inputText: "Weekly shop"
- tapOn: "Save"
- extendedWaitUntil:
    visible: "OK"
    timeout: 15000
- tapOn: "OK"
- tapOn:
    id: "BackButton"
- extendedWaitUntil:
    visible: "Templates"
    timeout: 15000
- takeScreenshot: /tmp/asc_05_templates
# ── Plans ──
- tapOn: "Plans"
- tapOn: "New weekly plan"
- inputText: "This week"
- pressKey: Enter
- extendedWaitUntil:
    visible: "This week"
    timeout: 15000
- hideKeyboard
- tapOn: "This week"
- scrollUntilVisible:
    element: "Entry title"
    direction: DOWN
- tapOn: "Entry title"
- inputText: "Pasta night"
- pressKey: Enter
- tapOn: "Entry title"
- inputText: "Bin collection"
- pressKey: Enter
- tapOn: "Entry title"
- inputText: "Chloe swimming 6pm"
- pressKey: Enter
- hideKeyboard
- scrollUntilVisible:
    element: "Pasta night"
    direction: UP
- takeScreenshot: /tmp/asc_06_plan
- tapOn:
    id: "BackButton"
# ── Security ──
# Two screens, not one: the Face ID toggle (1.5.0) lives in Settings, while
# the encryption story lives one level down in Privacy & encryption. The
# toggle is also HIDDEN unless the device can evaluate a biometric policy, so
# the simulator must have Face ID enrolled before this runs — that is
# make_screenshots.sh's job, and this step failing to find the row almost
# always means enrolment did not happen, not that the app changed.
- tapOn: "Settings"
# Privacy first, and centred before tapping. The tab bar floats OVER the list,
# so a row that is technically "visible" but sitting under it hands the tap to
# the tab bar instead: Maestro reports COMPLETED and the app quietly jumps to
# Checklists. Centring keeps the row clear of it. Seen 2026-08-14.
- scrollUntilVisible:
    element: "Privacy & encryption"
    direction: DOWN
    centerElement: true
- tapOn: "Privacy & encryption"
- extendedWaitUntil:
    visible: "Encrypted before upload"
    timeout: 15000
- takeScreenshot: /tmp/asc_09_privacy
- tapOn:
    id: "BackButton"
- extendedWaitUntil:
    visible: "Privacy & encryption"
    timeout: 15000
- scrollUntilVisible:
    element: "Require Face ID"
    direction: DOWN
    centerElement: true
- takeScreenshot: /tmp/asc_08_lock
# ── Connect an AI (1.7.0) ──
# Captured here because we are already in Settings; numbered 02 because the
# headline feature of this release belongs near the front of the listing.
#
# The code on screen is real, and that is fine: it belongs to a throwaway
# guest household on a simulator that gets erased, it is single-use, and it
# is dead ten minutes after capture. Showing the empty state instead would
# be safer in a way that protects nothing and explains nothing.
- scrollUntilVisible:
    element:
      id: "settings_connect_ai"
    direction: DOWN
    centerElement: true
- tapOn:
    id: "settings_connect_ai"
- extendedWaitUntil:
    visible:
      id: "ai_link_create"
    timeout: 45000
- tapOn:
    id: "ai_link_label_field"
- inputText: "Claude on my laptop"
- hideKeyboard
- tapOn:
    id: "ai_link_create"
- extendedWaitUntil:
    visible:
      id: "ai_link_code_value"
    timeout: 30000
- takeScreenshot: /tmp/asc_02_connect_ai
- tapOn:
    id: "BackButton"
- extendedWaitUntil:
    visible: "Privacy & encryption"
    timeout: 15000
# ── Search ──
# Deliberately LAST, though it is numbered 06. .searchable presents its field
# over the whole screen including the tab bar, and a tap on another tab while
# it is up is swallowed: Maestro reports COMPLETED, the app never moves, and
# every later step runs against the wrong screen. Cost an hour on 2026-08-14.
# Filenames set the order on the product page, not capture order, so this can
# run last and still appear sixth.
- tapOn: "Search"
- tapOn: "Search everything"
# "week" spans a task and a plan; a term that matches one row makes search
# look like it found almost nothing.
- inputText: "week"
- extendedWaitUntil:
    visible: "Weekly food shop"
    timeout: 25000
- hideKeyboard
- takeScreenshot: /tmp/asc_07_search
